version: v2

modules:
  - path: proto
    name: buf.build/kubling/kubling-grpc

lint:
  use:
    - STANDARD

breaking:
  use:
    - FILE
  ignore_only:
    FILE_SAME_GO_PACKAGE:
      - proto/kubling/v1